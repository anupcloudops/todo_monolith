name: test pipeline
on:
  workflow_dispatch
jobs:
  hello:
    runs-on: self-hosted
    steps:
      - name: hello-step
        run: echo "JAI JAGNNATH"
      - name: variable check
        run: echo "${{ git.actor}}"