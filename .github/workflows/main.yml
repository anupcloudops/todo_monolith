name: Hello World
on: workflow_dispatch
jobs:
  hello:
    runs-on: self-hosted
    steps:
      - name: hello-step
        uses: actions/checkout@v4
        run: |
          echo "JAI JAGNNATH"
          echo "JAI RADHA MAA"
  TELLO:
    runs-on: self-hosted
    steps:
      - name: hello-steped
        uses: actions/checkout@v4
        run: cat azure-pipelines.yml
