name: read file
on:
  workflow_dispatch
jobs:
  fileread:
    runs-on: self-hosted
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: read file
        run: cat azure-pipelines.yml
